import {
  VIEW_LIST_HOME
} from '../constants/application';

const config = {
    applicationTitle: 'Admin Panel',
    view: {
      [VIEW_LIST_HOME]: {
        pageTitle: 'Dashboard',
      }
    }
};

export default config;
